# Создать образ на основе базового слоя python (там будет ОС и интерпретатор Python).
# 3.7 — используемая версия Python.
# slim — обозначение того, что образ имеет только необходимые компоненты для запуска,
# он не будет занимать много места при развёртывании.
FROM python:3.7-slim

# Сделать директорию /app внутри контейнера рабочей директорией (если ее нет, она создастся)
WORKDIR /app

# Скопировать с локального компьютера все файлы из корневого каталога
COPY . .

# Выполнить установку зависимостей внутри контейнера.
RUN pip3 install -r api_yamdb/requirements.txt --no-cache-dir

# Сделать директорию api_yamdb внутри директории /app рабочей директорией. 
WORKDIR api_yamdb/

# Выполнить запуск сервера разработки при старте контейнера.
CMD ["gunicorn", "api_yamdb.wsgi:application", "--bind", "0:8000"]
